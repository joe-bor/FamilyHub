Create a new React component called $ARGUMENTS in src/components/

Follow these patterns from the codebase:
- Use TypeScript with proper interface definitions
- Use cn() from @/lib/utils for className merging
- Use Tailwind CSS classes matching our design system
- Export from the component file

Reference existing components in src/components/ui/ for patterns.
Reference src/index.css for color variables (--color-coral, --color-teal, etc.)

Create the component file and show me the result.
